# Independent longitudinal video processing

The entry point delegates unchanged to the archived evaluator. Run evaluate_video.py --help and supply local dataset/model paths and a NEW output directory. Never overwrite the archived P005 results. This stage performs no segmentation training or threshold derivation.
